# Lua Obfuscator Requirements
# Core functionality has NO external dependencies!

# Only needed for GUI (usually comes with Python)
# If tkinter is not available, install python3-tk:
# Ubuntu/Debian: sudo apt-get install python3-tk
# Fedora: sudo dnf install python3-tkinter
# macOS: Comes with Python
# Windows: Comes with Python

# Optional: for better performance with large files
# pip install -r requirements.txt

# No PyPI packages required for core functionality!
# This obfuscator is designed to work with standard Python library only.

# Optional enhancements (not required):
# colorama==0.4.6  # For colored terminal output
# tqdm==4.66.1     # For progress bars in batch processing
